<form method="post" action="{{ path('app_exercise_delete', {id: exercise.id}) }}" onsubmit="return confirm('Supprimer cet exercice ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exercise.id) }}">
    <button type="submit">Supprimer</button>
</form>
