{# Réponse Turbo Stream d'une mutation de la trame : on remplace intégralement
   le conteneur de la grille par sa version fraîchement rendue. Contexte fourni
   par gridContext(). #}
<turbo-stream action="replace" target="plan-grid">
    <template>
        {{ include('plan_template/_grid.html.twig') }}
    </template>
</turbo-stream>
