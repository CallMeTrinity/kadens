{% extends 'base.html.twig' %}

{% set workout = flat.workout %}

{% block title %}{{ workout.title }}{% endblock %}

{% block body %}
    {# Page publique en lecture seule : réutilise le composant de rendu, sans
       aucune action d'édition ni navigation propriétaire. Le bloc `actions` du
       composant reste vide, c'est ce qui garantit qu'aucune commande
       propriétaire ne peut fuiter ici. #}
    {% include 'components/_workout_read.html.twig' with {
        flat: flat, summary: summary, blockStats: blockStats
    } only %}
{% endblock %}
