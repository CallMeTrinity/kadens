<form method="post" action="{{ path('app_workout_delete', {id: workout.id}) }}" onsubmit="return confirm('Supprimer cette séance ?');" data-turbo="false">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ workout.id) }}">
    <button type="submit">Supprimer la séance</button>
</form>
